/**
 * TEST FIXTURES ONLY.
 *
 * These strings are small, synthetic samples for embedding tests and quality
 * evaluation. They are not real customs regulations, carrier policies, or
 * production logistics data.
 */

export const FIXTURE_SOURCE = "backend/scripts/fixtures/embedding-samples.ts";

/** TEST FIXTURE: lithium-battery shipping documentation (synthetic). */
export const FIXTURE_LITHIUM_DOCS =
  "TEST FIXTURE: Lithium battery shipments require a declaration form, a safety data sheet, and proof of packaging compliance before export.";

/** TEST FIXTURE: closely related lithium-battery handling text (synthetic). */
export const FIXTURE_LITHIUM_RELATED =
  "TEST FIXTURE: Before shipping lithium cells, the sender must declare the batteries, attach safety documents, and confirm compliant packaging.";

/** TEST FIXTURE: unrelated warehouse temperature text (synthetic). */
export const FIXTURE_UNRELATED_WAREHOUSE =
  "TEST FIXTURE: Palletized dry goods should be stored in a climate-controlled warehouse aisle with humidity logs updated twice per shift.";

/** TEST FIXTURE: short query-style sample (synthetic). */
export const FIXTURE_LITHIUM_QUERY =
  "TEST FIXTURE: What documents are needed to ship lithium batteries?";
